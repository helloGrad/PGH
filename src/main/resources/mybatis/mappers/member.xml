<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<!-- 정예린 박가혜 -->

	<insert id="insert" parameterType="memberVo">
		<![CDATA[	
			insert into MB (MB_NO, MB_DSTNCT, GENAL_MB_DSTNCT, NKNM, IDEN, PW, MYSELF_AUTHZ_YN, INSRD_DD, WTHDR_DD )
			values (null, '일반회원','일반가입',#{nknm },#{iden },password(#{pw }), 'N',date_format(now(),'%Y%m%d'),'99991231')
		]]>
	</insert>

	<insert id="snsinsert" parameterType="map">
		<![CDATA[
			insert into MB (MB_NO, MB_DSTNCT, MB_NM, NKNM, IDEN, SEX, MYSELF_AUTHZ_YN, INSRD_DD, WTHDR_DD, GENAL_MB_DSTNCT, SNS_TKN_VALUE, SNS_DSTNCT
		]]>
		<choose>
			<when test="snsnm =='fb'">
				<![CDATA[
					, BIRDT)
				]]>
			</when>
			<when test="snsnm =='naver'">
				<![CDATA[
					, AGRG)  
				]]>
			</when>
			<when test="snsnm =='google'">
				<![CDATA[
					  )
				]]>
			</when>
		</choose>
	<![CDATA[
	 		values (null,'일반회원', #{memberVo.mbNm }, #{memberVo.nknm }, #{memberVo.iden }, #{memberVo.sex },'Y',date_format(now(),'%Y%m%d'),'99991231','SNS가입', #{memberVo.snsTknValue },		
		]]>
		<choose>
			<when test="snsnm =='fb'">
				<![CDATA[
					'페이스북', #{memberVo.birdt })
				]]>
			</when>
			<when test="snsnm =='naver'">
				<![CDATA[
					'네이버', #{memberVo.agrg })
				]]>
			</when>
			<when test="snsnm =='google'">
				<![CDATA[
					'구글')
				]]>
			</when>
		</choose>
	</insert>


	<select id="getByToken" parameterType="memberVo" resultType="memberVo">
	<![CDATA[
		select * 
		from MB 
		where SNS_TKN_VALUE='${snsTknValue }'		
	]]>
	</select>

	<select id="getByIden" parameterType="string" resultType="memberVo">
	<![CDATA[
	
		select * 
		from MB 
		where IDEN=#{iden }		
	]]>
	</select>

	<select id="getByLoginInfo" parameterType="map" resultType="memberVo">  <!-- 객체면 변수 이름으로 해야함 uservo 일경우 vo로, 반환할 타입이 마땅히 없는 경우 map -->
		<![CDATA[
		select MB_NO as "mbNo",IDEN as "iden",MB_DSTNCT as "mbDstnct",NKNM as "nknm" 
		from mb 
		where IDEN=#{iden } 
		and PW=password(#{pw })
		]]>
	</select>

	<select id="getByCode" parameterType="long" resultType="codeVo">  
		<![CDATA[
		select cd.CD_ID as "cdId",CD_NM as "cdNm",CD_DSTNCT as "cdDstnct"
		from mb_info mi,CD 
		where mi.CD_ID = CD.CD_ID 
		and MB_NO =#{mbNo } 
		]]>
	</select>

	<insert id="insertMbinfo" parameterType="map">
	<![CDATA[
		insert into mb_info values (#{mbNo }, #{information })
	]]>
	</insert>
	
	<delete id="deleteMbinfo" parameterType="long">
	<![CDATA[	
		delete from mb_info where MB_NO=#{mbNo }		
	]]>
	</delete>

</mapper>
